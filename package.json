{
  "name": "lerna-monorepo",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/**/*"
    ]
  },
  "scripts": {
    "analyze": "lerna run analyze --stream --concurrency 1",
    "clean": "rm package-lock.json && lerna clean",
    "clean:packages": "lerna exec -- rm -rf ./node_modules && pnpm run clean:packages:dev && pnpm run clean:packages:coverage",
    "clean:packages:coverage": "lerna exec -- rm -rf ./test/coverage",
    "clean:packages:dev": "lerna exec -- rm -rf ./dev && lerna exec -- rm -rf ./package-lock.json",
    "eslint": "eslint --fix 'packages/**/*.*'",
    "format": "lerna run format --stream --concurrency 1",
    "format:pre-push": "pnpm run format -- --since",
    "lint": "pnpm run eslint && pnpm run prettier && pnpm run stylelint",
    "prepare": "husky",
    "prettier": "prettier --write --ignore-path .prettierignore 'packages/**/*.{js,ts,css,scss,html,json}'",
    "push": "git push --follow-tags origin main",
    "release": " npm ci && pnpm run test:pre-push && lerna version --no-push",
    "site": "pnpm run site:prepare && pnpm run site:copy",
    "site:copy": "rm -rf ./site/**/* && node scripts/move-dev-directory.js",
    "site:prepare": "pnpm run clean:packages:dev && lerna run dev:vite",
    "standard": "commit-and-tag-version",
    "stylelint": "stylelint -f verbose --ignore-path .prettierignore 'packages/**/*.{js,ts,css,scss,html}'",
    "test": "lerna run test --stream --concurrency 1 -- --run",
    "test:pre-push": "lerna run test --stream --concurrency 1 --since origin/main -- --run",
    "test:scope": "func() { lerna run test --stream --concurrency 1 --scope @blockquote-playground/${1}; }; func",
    "postversion": "pnpm run standard"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional",
      "@commitlint/config-workspace-scopes"
    ],
    "rules": {
      "header-max-length": [
        2,
        "always",
        150
      ]
    }
  },
  "prettier": {
    "bracketSameLine": true,
    "bracketSpacing": false,
    "htmlWhitespaceSensitivity": "ignore",
    "printWidth": 100,
    "singleQuote": true,
    "trailingComma": "es5",
    "overrides": [
      {
        "files": "*.{scss,css}",
        "options": {
          "singleQuote": false
        }
      },
      {
        "files": "*.json",
        "options": {
          "parser": "json-stringify"
        }
      }
    ]
  },
  "stylelint": {
    "extends": "stylelint-config-standard-scss",
    "allowEmptyInput": true,
    "rules": {
      "custom-property-pattern": null,
      "no-duplicate-selectors": null,
      "color-function-notation": null,
      "alpha-value-notation": null,
      "media-feature-name-no-unknown": null,
      "comment-empty-line-before": null,
      "declaration-empty-line-before": null,
      "property-no-vendor-prefix": null,
      "selector-attribute-quotes": null,
      "value-no-vendor-prefix": null
    },
    "overrides": [
      {
        "files": [
          "**/*.html"
        ],
        "customSyntax": "postcss-html"
      },
      {
        "files": [
          "**/*.{ts,js}"
        ],
        "customSyntax": "postcss-lit"
      }
    ]
  },
  "overrides": {
    "glob": "*"
  },
  "dependencies": {
    "linkedom": "^0.18.12"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^20.4.2",
    "@commitlint/config-workspace-scopes": "^20.4.0",
    "@eslint/js": "^10.0.1",
    "commit-and-tag-version": "^12.6.1",
    "eslint": "^10.0.2",
    "husky": "^9.1.7",
    "lerna": "^9.0.4",
     "globals": "^17.3.0",
    "lint-staged": "^16.3.0",
    "prettier": "^3.8.1",
    "stylelint": "^17.4.0",
    "stylelint-config-standard-scss": "^17.0.0"
  },
  "commit-and-tag-version": {
    "releaseCommitMessageFormat": "chore: release {{currentTag}}",
    "commitUrlFormat": "{{host}}/oscarmarina/lerna-netlify-test/commit/{{hash}}",
    "compareUrlFormat": "{{host}}/oscarmarina/lerna-netlify-test/compare/{{previousTag}}...{{currentTag}}"
  }
}
